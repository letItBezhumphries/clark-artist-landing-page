import React, { Fragment } from 'react';

const ImageCard = ({ image }) => (
  <Fragment>
    {/* <img src={image.imageUrl} alt="" className={props.cardType + "-card__img"} />
    <h1 className="portfolio-card__title">{image.portfolio}</h1> */}
    An ImageCard
  </Fragment>
);

export default ImageCard;
